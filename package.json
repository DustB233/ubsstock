{
  "name": "china-outbound-stock-ai-analyzer",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:web": "cd apps/web && npm run dev",
    "build:web": "cd apps/web && npm run build",
    "lint:web": "cd apps/web && npm run lint",
    "typecheck:web": "cd apps/web && npm run typecheck",
    "dev:api": "cd apps/api && uv run uvicorn china_outbound_analyzer.main:app --reload --host 0.0.0.0 --port 8000",
    "test:api": "cd apps/api && uv run pytest",
    "api:docs": "cd apps/api && uv run python -m china_outbound_analyzer.main"
  }
}

